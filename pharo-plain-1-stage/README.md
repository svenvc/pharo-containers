# Pharo Containers
## pharo-plain-1-stage

This is a very simple Dockerfile with no optimizations.

The `build.st` file is a Pharo script to load application code.

The `run.st` file is a Pharo script to start the application.
